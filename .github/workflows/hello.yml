name : Hello
on:
  workflow_dispatch: 

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:  
      - name: print user
        run: echo "hello $USER"
      - name: say hello to username
        run: echo "Hello $USER"
